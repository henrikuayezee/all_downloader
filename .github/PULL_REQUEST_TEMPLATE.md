## What changed

<!-- Describe the change and why it's needed. -->

## Checklist

- [ ] Follows the conventions in [CLAUDE.md](../CLAUDE.md) — no build step,
      no dependencies, ES5-flavoured JS in `index.html`, design tokens only
- [ ] Tested locally with `python3 -m http.server 8000`
- [ ] Manually verified the affected platform(s) (Twitter/X, Instagram,
      Facebook) still work, including at least one failure path (private
      post, bad link, etc.)
- [ ] Updated `README.md` / `CLAUDE.md` if this changes behavior they
      document
